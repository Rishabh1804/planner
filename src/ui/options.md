# Options UI

Options-screen UI belongs here once the bundle is split.

## Owns

- ranked destination cards
- destination comparison board
- stay strategy rendering
- budget breakdown rendering
- shortlist display

## Rule

Options should render derived recommendations. It should not contain separate hard-coded trip assumptions that drift from Planner state.
